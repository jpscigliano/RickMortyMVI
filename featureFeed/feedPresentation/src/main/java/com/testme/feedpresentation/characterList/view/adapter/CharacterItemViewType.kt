package com.testme.feedpresentation.characterList.view.adapter

import com.testme.presentation.adapter.ViewType

enum class CharacterItemViewType(override val value: Int) : ViewType {
  CHARACTER(value = 0),
}